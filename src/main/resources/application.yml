spring:
  datasource:
    url: jdbc:mysql://qma-db.cpu60suyunet.ap-south-1.rds.amazonaws.com:3306/qma_db
    username: admin
    password: Pranshi123
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    user:
      name: ${ADMIN_USERNAME}
      password: ${ADMIN_PASSWORD}
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}

security:
  jwt:
    secret: ${JWT_SECRET}